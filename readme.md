base neo4j db image for Freshit

image stored on `587250238214.dkr.ecr.us-east-1.amazonaws.com/freshit-neo4j-db`

default auth credentials are:
```
username = "neo4j"
password = "root"
```
- should be overridden in any non-dev environments
